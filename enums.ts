/* eslint-disable @typescript-eslint/naming-convention */

// Typo 아톰에서 사용하는 글자 크기 관련 enum 타입입니다.
export enum TYPO_SIZE {
  LARGE = "large",
  MEDIUM = "medium",
  SMALL = "small",
  TINY = "tiny",
}
